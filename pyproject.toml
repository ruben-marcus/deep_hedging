[project]
name = "deep-hedging"
version = "0.1.0"
description = "deep hedging experiments: neural policies vs black-scholes delta"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.13",
    "torch>=2.4",
    "pandas>=2.0",
    "matplotlib>=3.8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["portfolio", "train", "metrics", "losses", "experiment"]

[tool.setuptools.packages.find]
where = ["src"]